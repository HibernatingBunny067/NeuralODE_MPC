# Model Predictive Control using NeuralODE on analytical Ball-on-Beam Plant, a proof of concept strategy for Data Driven Control of Unknown Dynamical Systems
-----

**Python 3.12.2 recommended**
```
#create a virtual environment
pip install -r REQUIREMENTS.txt

#run this for creating the Box-Plot Validation
python src/Box_Plot_Validation.py --nCases <number of initial conditions, defaults to 30>
```